name: mnist
type: ddit
hidden_size: 512
cond_dim: 128
# input_ids = [conditioning top-half image | full image label] => 2 * (14*14) = 392
length: 392
n_blocks: 4
n_heads: 4
scale_by_sigma: True
dropout: 0.1
tie_word_embeddings: False
vocab_lookup: True